const setup_param = {
    backend_api_endpoint: `http://${document.location.hostname}:9010/api`,
    default_email_domain: 'ha.org.hk',
    default_loading_message: {
        status: 'loading',
        message: 'Loading, please wait...',
      },
}

export default setup_param;